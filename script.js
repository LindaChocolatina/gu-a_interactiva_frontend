// Se ejecuta cuando el DOM está listo
document.addEventListener("DOMContentLoaded", function () {
    console.log("DOM cargado correctamente 🚀");

    // Prueba visible (opcional)
    alert("Tu JavaScript está funcionando ✨");
});
